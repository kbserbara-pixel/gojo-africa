fastapi>=0.115.0
uvicorn[standard]>=0.30.6
sqlalchemy>=2.0.35
pydantic[email]>=2.9.2
pydantic-settings>=2.5.2
python-dotenv==1.0.1
passlib[bcrypt]==1.7.4
# passlib 1.7.4 (unmaintained since 2020) breaks on bcrypt>=4.1 -- it calls a
# bcrypt internal attribute (`__about__.__version__`) that newer bcrypt
# releases removed, which makes every single password hash/verify call raise
# "password cannot be longer than 72 bytes" regardless of actual password
# length (it's failing in passlib's own internal self-test, not on real
# input). Pin bcrypt itself to the last version passlib still works with.
bcrypt==4.0.1
python-jose[cryptography]==3.3.0
redis==5.0.8
celery==5.4.0
openai==1.51.0
python-multipart==0.0.9

# Required now that DATABASE_URL points at Supabase Postgres instead of the
# local SQLite default. Always install with --only-binary so pip uses the
# prebuilt wheel instead of compiling from source (the from-source build
# needs Postgres dev headers that most Windows/Python setups don't have):
#   pip install psycopg2-binary --only-binary :all:
psycopg2-binary==2.9.9

# Phone number parsing/normalization for email-or-phone login & sign-up
# (recognizes country codes, e.g. +251.../+1.../+44..., not just local format).
phonenumbers>=8.13.0

# Used to upload listing photos to Supabase Storage's REST API.
httpx>=0.27.0
